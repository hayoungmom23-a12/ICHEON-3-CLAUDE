# 인수인계 메모 (새 대화에서 이어가기용)

작성: 2026-09-18. 새 대화창에서 이 파일을 먼저 읽고 "다음 할 일"부터 이어간다.

## 프로젝트

- 이름: 이천 아이맵. 이천이 낯선 미취학·저학년 아이 동반 가족이 출발 전에 코스 카드에서 주차장·진입 방향·운영시간·공식 링크를 확인하는 모바일 웹페이지.
- 폴더: `C:\Users\user\Desktop\바이브코딩\ICHEON_claude`
- 깃허브: https://github.com/hayoungmom23-a12/ICHEON-3-CLAUDE (공개, main 브랜치, 커밋 작성자는 저장소 로컬 설정 hayoungmom23-a12 / hayoungmom23@gmail.com)
- 과정: vibe 플러그인. 순서 `/vibe:01-prd` → `02-design` → `03-frd` → `04-trd` → `05-plan` → `build`. 진행 모드는 `docs/setup.md`에 "꼼꼼"으로 적혀 있다.

## 진행 상태

| 단계 | 문서 | 상태 |
|---|---|---|
| 1 PRD | `docs/prd.md` | 완료 (2026-09-18 디자인 결정 반영해 갱신) |
| 2 디자인 | `docs/design-prompt.md` | 완료 (2026-09-18 확정) |
| 3 FRD | `docs/frd.md` | 미시작 |
| 4 TRD | `docs/trd.md` | 미시작 |
| 5 계획 | `docs/plan.md` | 미시작 |

## 디자인 단계에서 확정된 것

- 구현 기준 시안: Claude Design 앱에서 만든 PNG 3장 `design/01-코스목록.png`, `design/02-코스상세.png`, `design/03-하단안내.png`.
- 무드: 화이트 체크카드 + 청록 채움 버튼 + 코스 목록 맨 위 진초록 그라데이션 히어로. 바탕 흰색. 값은 `docs/design-prompt.md` 5절(색·글자·간격·컴포넌트 표).
- 참고 자료: `design/old/` (2026-09-16 Claude Code 캔버스 시안 HTML 4장, 클릭형 프로토타입 `05-프로토타입.html`, 캔버스 작업 파일, Stitch·Claude Design용 프롬프트). 캔버스 링크 https://claude.ai/artifact/JFjU1gw62vBnanHWHNmZ8i
- 시안으로 PRD에 추가된 요소: 코스 요약 목록("코스 5개 한눈에"), 바깥 링크 타일 2개(이천시 공식 블로그, 이천관광 누리집), 스팟 카드 3분할 탭(추천 장소 / 방문 팁 / 약도 보기), 스팟별 방문자 시점 약도 이미지(선택), ★ 추천 배지, 줄 썸네일, 상시 개방 시설의 "지도 열기" 버튼.
- 사진 규칙: 직접 촬영 우선, 없으면 위키미디어 공용 등 CC 사진을 출처·저작자·라이선스 표기와 함께 쓴다.
- 이름 확인됨: 이천농업테마공원 추천 주차장 "안내센터 옆 주차장"(남부 코스 2개 공용), 시설 "쌀문화전시관".
- 프로토타입(`design/old/05-프로토타입.html`)의 JS 데이터 객체가 코스 5개·스팟 5곳 데이터 구조의 초안이다. FRD의 데이터 항목 정의에 그대로 쓸 수 있다.

## 아직 정하지 않은 것

- 진행 모드: 학습자가 FRD·TRD를 건너뛰고 싶어 했으나 과정 도구상 불가. 추천은 `docs/setup.md`의 진행 모드를 "간단"으로 바꿔 3·4단계를 각 2라운드로 짧게 끝내는 것(선택지 A). 학습자 답을 아직 받지 못했다.
- 이천농업테마공원의 두 번째 주차장 이름, 남부 만들기 코스에서 가는 공원 시설 이름 (구현 데이터 작성 때 답사 메모로 확정).
- 구현 완성과 배포의 새 목표일. PRD의 09-17/09-18은 지났다. 3단계에서 다시 정한다.
- 네이버지도 임베드 키 발급 여부 (PRD 조건부 기능). 없으면 지도 자리를 뺀다.

## 다음 할 일 (순서대로)

1. 진행 모드 결정. "간단"이면 `docs/setup.md`의 `진행 모드: 꼼꼼`을 `진행 모드: 간단`으로 바꾼다.
2. `/vibe:03-frd` 실행. R1에서 `design/*.png` 3장을 읽어 시안과 PRD를 대조한다. 위 "확정된 것"의 추가 요소가 PRD에 이미 반영되어 있다.
3. `/vibe:04-trd` 실행. 서버·로그인 없는 정적 사이트(HTML·CSS·JS + JSON 3개 + 이미지 폴더), 배포는 GitHub Pages가 자연스럽다.
4. `/vibe:05-plan` 실행 → `/vibe:build`.
5. 문서가 바뀔 때마다 커밋·푸시한다 (과제 제출용).

## 환경 메모

- Node 24 있음, Python 없음. `gh`는 hayoungmom23-a12 계정으로 로그인됨.
- 브라우저 도구의 화면 캡처가 자주 실패한다. 페이지 글 읽기(get_page_text)와 JS 실행으로 확인하는 편이 안정적이다.
- 로컬 미리보기 서버 설정 `.claude/launch.json`(이름 design-preview, 포트 8765)이 있으나 `.claude/`는 git에 올리지 않는다. 스크립트는 세션 임시 폴더에 있어 새 대화에서는 다시 만들어야 할 수 있다.
- 학습자는 비개발자다. 어려운 용어에는 한 줄 풀이를 붙이고, 파일 조작은 폴더 위치와 클릭 순서까지 단계별로 안내한다. 글보다 그림으로 비교해 보여주면 이해가 빠르다.

## 새 대화 첫 메시지 예시

```
docs/handoff.md를 읽고 이어서 진행해줘. 진행 모드는 간단으로 바꾸고 /vibe:03-frd를 시작하자.
```
